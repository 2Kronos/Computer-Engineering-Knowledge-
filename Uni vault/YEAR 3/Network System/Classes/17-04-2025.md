- use a program use routing
- When you want to push data onto different networks you will use a program for each router.
- Each router must know where to push information at which port
- You must do this for all the routers on the different network 
- You must do it in the config mode
- To test the system  their will send packets to different servers
- When you ping your router it will know the location of the other networks

- Configure between routers before continue 

- Must know the network and the subnet mast for the router 
- You need the information of the network
- You also need the command 

```
1.1.            R1(config)#ip route 10.10.10.12 255.255.255.252 s0/0/0 150///
```

`10.10.10.1` This is the ip for the network
`255.255.255.252 ` This is the subnetmask for the network
`s0/0/0 150///` this will be the interface it will come out from 

- You do more or less on the second router when your are pinging it back to the first router 
- This is how PC1 will be able to able to ping to the second router 

- For the third network we must tell it to push to the same network as for the second router 
