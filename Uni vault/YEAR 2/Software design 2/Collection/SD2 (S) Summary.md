# Collection test

```java
-creating a Array to hold string
-create a list from the arraylist class using the LIST interface
-iterate threw the array to add the array items in the list

-Create a array for the colours you want to remove
-create a list from the arraylist class using the LIST interface for the array for the array of strings you just created
-iterate threw the array to add the array items in the list

-print out the array to indicate how it looks before removal
- send the two list to a method that will remove the string you want from the firts lsist
- The create an iterator that will iterate your first list
- create a loop that will run only if list 1 has items (hasNext) and check if any thing in your second list is found in your first lie 
- using the iterator for list one remove any matching items

```

# Linked list summary

```java
Here’s a summary of the provided code:

1. **Create an Array to Hold Strings**:
    
    - Initialize an array `colors` with color names.
    - Initialize an array `colors2` with another set of color names.
2. Create Lists from Arrays**:
    
    - Create `list1` from the `LinkedList` class using the `List` interface.
    - Iterate through the `colors` array to add its items to `list1`.
    - Create `list2` from the `LinkedList` class using the `List` interface.
    - Iterate through the `colors2` array to add its items to `list2`.
3. **Concatenate Lists**:
    
    - Add all elements from `list2` to `list1`.
    - Set `list2` to `null` to release resources.
4. **Print List**:
    
    - Print the elements of `list1` to show its contents before any modifications.
5. **Convert to Uppercase**:
    
    - Convert all strings in `list1` to uppercase using the `convertToUppercaseStrings` method.
    - Print the elements of `list1` again to show the uppercase conversion.
6. **Remove Items**:
    
    - Print a message indicating the removal of elements.
    - Remove elements from index 4 to 6 in `list1` using the `removeItems` method.
    - Print the elements of `list1` after the removal.
7. **Print Reversed List**:
    
    - Print the elements of `list1` in reverse order using the `printReversedList` method.

Would you like any further details on any specific part of the code?
```

# List of Array Summary

```Java
Here’s a summary of the provided code:

1. Create an array to hold strings.
    
    - Initialize an array `colors` with color names.
2. Create a list from the array using the `LinkedList` class.
    
    - Convert the `colors` array into a `LinkedList` named `links`.
3. Add elements to the `LinkedList`.
    
    - Add “red” as the last item.
    - Add “pink” to the end.
    - Insert “green” at the 3rd index.
    - Add “cyan” as the first item.
4. Convert the `LinkedList` back to an array.
    
    - Convert `links` to an array named `colors`.
5. Print the array elements.
    
    - Print each element of the `colors` array to the console.

Would you like any further details on any specific part of the code?

```